<%- include('partials/header') %>

<div class="d-flex justify-content-between align-items-center mb-3">
    <h1 class="h2 mb-0">Referensi Kode Kategori & Sub Kategori</h1>
    <!-- BARU: Tombol Download -->
    <a href="/download-referensi" class="btn btn-success">
        <i class="bi bi-file-earmark-excel-fill"></i> Download Referensi (Excel)
    </a>
    <!-- AKHIR BARU -->
</div>


<p>Gunakan tabel ini sebagai acuan saat mengisi data inventaris, terutama untuk kolom Sub Kategori pada template Excel.</p>

<!-- ... (Sisa kode accordion dan tabel tidak berubah) ... -->
<div class="accordion" id="referensiAccordion">
    <% referensiData.forEach((kategori, index) => { %>
        <div class="accordion-item">
            <h2 class="accordion-header" id="heading<%= index %>">
                <button class="accordion-button <%= index !== 0 ? 'collapsed' : '' %>" type="button" data-bs-toggle="collapse" data-bs-target="#collapse<%= index %>" aria-expanded="<%= index === 0 ? 'true' : 'false' %>" aria-controls="collapse<%= index %>">
                    <strong><%= kategori.namaKategori %></strong>
                </button>
            </h2>
            <div id="collapse<%= index %>" class="accordion-collapse collapse <%= index === 0 ? 'show' : '' %>" aria-labelledby="heading<%= index %>" data-bs-parent="#referensiAccordion">
                <div class="accordion-body p-0">
                    <table class="table table-striped table-bordered mb-0 small">
                        <thead class="table-light">
                            <tr>
                                <th style="width: 35%;">Nama Sub Kategori</th>
                                <th style="width: 15%;">Kode Sub Kategori</th>
                                <th style="width: 50%;">Contoh Barang</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% kategori.subkategoris.forEach(sub => { %>
                                <tr>
                                    <td><%= sub.namaSubKategori %></td>
                                    <td><code><%= sub.kodeSubKategori %></code></td>
                                    <td><%= sub.contohBarang %></td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    <% }) %>
</div>

<a href="/" class="btn btn-secondary mt-4"><i class="bi bi-arrow-left-circle"></i> Kembali ke Daftar Utama</a>

<%- include('partials/footer') %>